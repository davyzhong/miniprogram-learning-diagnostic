.page {
  min-height: 100vh;
  padding: 20rpx 24rpx 48rpx;
  box-sizing: border-box;
  background: var(--b1-canvas);
}

.test-summary {
  padding: 18rpx 20rpx;
}

.test-summary-top,
.active-category-head,
.active-category-title-row,
.category-navigation,
.batch-overview,
.batch-tabs,
.batch-tab,
.batch-tab-main {
  display: flex;
  align-items: center;
}

.test-summary-top,
.active-category-head,
.category-navigation {
  justify-content: space-between;
}

.test-title,
.test-count,
.environment-text,
.active-category-meta {
  display: block;
}

.test-title {
  color: var(--b1-ink-strong);
  font-size: 30rpx;
  font-weight: 700;
}

.test-count,
.environment-text,
.active-category-meta {
  margin-top: 6rpx;
  color: var(--b1-ink-muted);
  font-size: 21rpx;
  line-height: 1.4;
}

.environment-label,
.verified-status {
  padding: 6rpx 10rpx;
  border-radius: 8rpx;
  background: var(--b1-improved-bg);
  color: var(--b1-improved-fg);
  font-size: 20rpx;
  font-weight: 600;
}

.batch-overview {
  gap: 8rpx;
  margin-top: 12rpx;
}

.batch-overview-item {
  flex: 1;
  padding: 8rpx 6rpx;
  border-radius: 8rpx;
  background: var(--b1-canvas);
  color: var(--b1-ink-muted);
  font-size: 20rpx;
  font-weight: 600;
  text-align: center;
}

.batch-overview-item.is-verified {
  background: var(--b1-improved-bg);
  color: var(--b1-improved-fg);
}

.batch-overview-item.is-pending {
  background: var(--b1-waiting-bg);
  color: var(--b1-waiting-fg);
}

.batch-tabs {
  gap: 10rpx;
  margin-top: 14rpx;
}

.batch-tab {
  min-width: 0;
  height: 76rpx;
  flex: 1;
  justify-content: space-between;
  gap: 8rpx;
  padding: 10rpx 12rpx;
  box-sizing: border-box;
  border: 1rpx solid var(--b1-border);
  border-radius: 10rpx;
  background: var(--b1-surface);
  color: var(--b1-ink-muted);
}

.batch-tab.is-active {
  border-color: var(--b1-informational-fg);
  background: var(--b1-informational-bg);
  color: var(--b1-informational-fg);
}

.batch-tab-main {
  min-width: 0;
  align-items: baseline;
  gap: 6rpx;
}

.batch-tab-name {
  font-size: 23rpx;
  font-weight: 700;
}

.batch-tab-status,
.batch-tab-id {
  font-size: 20rpx;
  white-space: nowrap;
}

.batch-tab-id {
  flex-shrink: 0;
  padding: 6rpx 8rpx;
  border-radius: 6rpx;
  background: rgba(255, 255, 255, .72);
  font-weight: 700;
}

.category-scroll {
  width: 100%;
  margin-top: 16rpx;
  white-space: nowrap;
}

.category-tabs {
  display: inline-flex;
  gap: 10rpx;
  padding: 2rpx 0 8rpx;
}

.category-tab {
  min-width: 138rpx;
  height: 76rpx;
  padding: 10rpx 14rpx;
  box-sizing: border-box;
  border: 1rpx solid var(--b1-border);
  border-radius: 10rpx;
  background: var(--b1-surface);
  color: var(--b1-ink-muted);
}

.category-tab.is-active {
  border-color: var(--b1-informational-fg);
  background: var(--b1-informational-bg);
  color: var(--b1-informational-fg);
}

.category-tab-id,
.category-tab-name {
  display: block;
  font-size: 20rpx;
  line-height: 1.25;
}

.category-tab-id {
  font-weight: 700;
}

.category-tab-name {
  margin-top: 4rpx;
  font-size: 20rpx;
}

.active-category-head {
  gap: 16rpx;
  margin-top: 16rpx;
}

.active-category-copy {
  min-width: 0;
  flex: 1;
}

.active-category-title-row {
  min-width: 0;
  gap: 10rpx;
}

.active-category-id {
  color: var(--b1-informational-fg);
  font-size: 26rpx;
  font-weight: 700;
}

.active-category-name {
  min-width: 0;
  color: var(--b1-ink-strong);
  font-size: 28rpx;
  font-weight: 700;
}

.copy-action {
  flex-shrink: 0;
  padding: 12rpx 14rpx;
  border-radius: 8rpx;
  background: var(--b1-informational-bg);
  color: var(--b1-informational-fg);
  font-size: 20rpx;
  font-weight: 600;
}

.risk-note {
  margin-top: 12rpx;
  padding: 12rpx 16rpx;
  border-left: 6rpx solid var(--b1-waiting-fg);
  background: var(--b1-waiting-bg);
  color: var(--b1-waiting-fg);
  font-size: 21rpx;
  line-height: 1.45;
}

.candidate-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 10rpx;
  margin-top: 14rpx;
}

.candidate-item {
  min-width: 0;
  height: 174rpx;
  padding: 12rpx 6rpx 10rpx;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  border: 1rpx solid var(--b1-border);
  border-radius: 10rpx;
  background: var(--b1-surface);
}

.candidate-item:active {
  border-color: var(--b1-informational-fg);
  background: var(--b1-informational-bg);
}

.candidate-glyph {
  min-height: 54rpx;
  font-size: 42rpx;
  line-height: 1.2;
}

.candidate-id {
  width: 100%;
  margin-top: 5rpx;
  overflow: hidden;
  color: var(--b1-informational-fg);
  font-size: 20rpx;
  font-weight: 700;
  line-height: 1.2;
  text-align: center;
  text-overflow: clip;
  white-space: nowrap;
}

.candidate-label {
  width: 100%;
  margin-top: 4rpx;
  min-height: 48rpx;
  overflow: hidden;
  color: var(--b1-ink-muted);
  font-size: 20rpx;
  line-height: 1.2;
  text-align: center;
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

.category-navigation {
  margin-top: 20rpx;
}

.category-nav-button {
  width: 176rpx;
  height: 72rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  box-sizing: border-box;
  border: 1rpx solid var(--b1-border);
  border-radius: 10rpx;
  background: var(--b1-surface);
  color: var(--b1-ink-strong);
  font-size: 23rpx;
  font-weight: 600;
}

.category-nav-button.is-next {
  border-color: var(--b1-informational-fg);
  background: var(--b1-informational-bg);
  color: var(--b1-informational-fg);
}

.category-nav-button.is-disabled {
  opacity: .38;
}

.category-nav-direction {
  font-size: 34rpx;
  line-height: 1;
}

.category-position {
  color: var(--b1-ink-subtle);
  font-size: 20rpx;
}

/* 第四批：emoji 符号 */
.test-symbol { font-size: 28rpx; margin-right: 6rpx; }
