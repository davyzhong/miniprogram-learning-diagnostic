const {
  FALLBACK_NAME,
  summarizeBottleneckName,
  uniqueBottleneckSummaries,
} = require('./bottleneck-name');

module.exports = {
  FALLBACK_NAME,
  summarizeBottleneckName,
  uniqueBottleneckSummaries,
};
