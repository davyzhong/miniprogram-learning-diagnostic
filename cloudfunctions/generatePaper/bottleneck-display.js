const {
  FALLBACK_NAME,
  summarizeBottleneckName,
  uniqueBottleneckSummaries,
} = require('../_shared/bottleneck-name');

module.exports = {
  FALLBACK_NAME,
  summarizeBottleneckName,
  uniqueBottleneckSummaries,
};
