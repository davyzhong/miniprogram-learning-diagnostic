# 第二批 Emoji 兼容性测试基线

> **2026-07-18 后续状态**：B02 已完成真机验收，996 个 Android/iOS 双端通过项以约 11 KB 紧凑生成表进入主包，4 个 Android 方格项被排除。当前源码主包检查为 788 KB，低于项目 800 KB 预算。结果详见 `docs/EMOJI_COMPATIBILITY_WHITELIST.md`。

## 范围

- 日期：2026-07-18（Asia/Shanghai）
- 第二批：`B02`，26 类，1,000 项
- 已验证第一批：`B01`，14 类，202 项
- 页面策略：默认打开 `B02-C01`，每次只向页面传入当前分类，最多 50 项
- 生产边界：第二批数据仅由图标兼容性测试分包控制器引用，不进入业务图标白名单

## 数据与生成基线

| 指标 | 结果 |
| --- | ---: |
| 权威清单 | 253,054 字节 |
| 紧凑运行时数据 | 54,706 字节 |
| 运行时相对清单缩减 | 78.4% |
| 第一批运行时数据 | 28,855 字节 |
| 测试分包五个核心源码文件合计 | 97,856 字节 |
| 单次最大活动分类 | 50 项 |

清单来源固定为 Unicode Emoji 17.0、Emoji Variation Sequences 17.0 和 CLDR 48.2。`npm run emoji:batch02:verify` 校验来源、哈希、配额、重复项与第一批重叠；`npm run emoji:batch02:runtime:verify` 校验生成文件无漂移。

## 主包源码基线

执行 `npm run check:size`：

| 指标 | 结果 |
| --- | ---: |
| 当前主包源码估算 | 773 KB |
| 仓库预算 | 800 KB |
| 剩余预算 | 27 KB |

该命令是仓库源码预算门禁，不等同于微信开发者工具编译包体。第二批 1,000 项位于独立分包，主包只增加首页入口文案。

## DevTools 编译基线

上一批已记录的 DevTools Preview 基线：总包 847,737 字节、主包 582,545 字节、图标测试分包 23,906 字节。第二批验收预算为：

- 编译主包相对 582,545 字节增量不超过 5 KB。
- 图标测试分包小于 250 KB。
- `--info-output` 缺少编译包字段时必须失败，不允许退回源码大小冒充编译结果。

本轮 `npm run perf:emoji-compat` 尚未取得新的编译数值。第一次运行被本地端口权限阻止；授权后，Preview 上传被工作环境的数据外发策略拒绝。该项状态为“未执行”，不是“通过”。脚本已保留，允许在用户本机终端直接复测并生成：

- `tmp/emoji-batch-02-preview-info.json`
- `tmp/emoji-batch-02-preview-qr.png`

## 渲染自动化

`tests/emoji-compatibility-devtools.test.js` 的 4 个解析与验收辅助测试已通过，覆盖：

- 编译总包、主包、图标分包的严格解析。
- 编译字段缺失时明确失败。
- 当前分类渲染数量与编号前缀一致。
- 分类切换后拒绝旧卡片残留，并检查两批独立位置恢复。

真实 `npm run test:e2e:emoji-compat` 已尝试运行，但微信开发者工具返回“HTTP 服务端口未开启”，因此页面自动化状态为“未执行”。在开发者工具“设置 > 安全设置”打开服务端口后重跑；脚本会遍历 `B02-C01` 至 `B02-C10`、`B02-C21`、`B02-C26`，验证 B01/B02 状态恢复，并输出四张截图。

## 当前结论

- 数据完整性、来源校验、生成稳定性、页面状态和隔离边界均已由自动测试覆盖。
- 完整验证通过：1,001/1,001 项单元测试成功，311 个 JavaScript 文件静态检查通过。
- 覆盖率门禁通过：全局行覆盖率 92.98%，函数覆盖率 86.61%。
- 主包源码估算仍在 800 KB 预算内，但只剩 27 KB，需要继续严控主包增长。
- 新的 DevTools 编译包体和真实模拟器截图仍待服务端口可用后复测，不能据现有结果宣称这两项已通过。
