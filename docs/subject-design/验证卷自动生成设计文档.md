# 验证卷自动生成设计文档

> 版本：v0.1
> 日期：2026-06-17
> 状态：历史设计稿（已被《验证卷完整设计文档》v3.2+ 修正）
> 定位：把验证卷从"用户手动点击生成"改为"诊断报告完成后后台自动生成"，用户只需预览、打印、上传作答。

> 2026-06-21 修订说明：
> - 当前权威实现以《验证卷完整设计文档》为准。
> - 前端不再驱动分批出题、不再创建兜底验证卷；`navigateToVerificationPaper` 只负责查状态、轮询和进入下载页。
> - 验证卷 ready 后主动作必须是"下载验证卷"，且允许重复下载；"上传作答照片"是独立二级动作。

## 一、背景与动机

### 当前流程的痛点

```
当前：上传→诊断报告→用户手动进卡点页→点"生成验证卷"→等LLM(60s)→预览→打印
```

1. **用户要主动触发**：报告页、学科工作台、卡点中心、卡点详情、首页共有 6 个入口都导向"手动生成验证卷"，用户需要理解"什么是验证卷"并主动点击。
2. **等待体验差**：LLM 生成试卷需要 30-60 秒，用户点击后必须等待，期间页面 loading。
3. **入口分散**：6 个入口（报告页×2、学科工作台、卡点中心、卡点详情、首页/档案页）的逻辑各自独立，维护成本高。

### 目标流程

```
改后：上传→诊断报告→（后台自动生成验证卷）→用户直接预览→打印→上传作答
```

1. **零等待**：用户看完报告想打印验证卷时，它已经生成好了。
2. **零理解成本**：用户不需要理解"生成验证卷"这个动作，只需"预览→打印"。
3. **解决超时**：异步生成不受前端 60 秒超时限制。

## 二、已确认的设计决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| **触发条件** | 有 `pendingBottlenecks` 就自动生成 | 没卡点就不浪费 LLM 调用 |
| **覆盖策略** | 新诊断覆盖旧卷（旧的标记 `superseded`） | 始终用最新诊断结果 |
| **失败处理** | 自动重试 N 次（3次，间隔递增） | 提高可靠性 |
| **状态获取** | 每页独立调 `getActiveVerificationPaper` | 简单直接，状态实时 |

## 三、自动生成引擎设计

### 3.1 触发时机

诊断报告分析完成后，在 `analyzePhotos/index.js` 的 `writeCompletedAnalysis` 函数结尾挂载：

```js
// analyzePhotos/index.js — writeCompletedAnalysis 结尾（行 631 后）
if (profileSummary.isEffective && (profile.pendingBottlenecks || []).length > 0) {
  triggerAutoVerificationPaper(reportId, studentId, subject, profile)
    .catch(err => console.warn('auto verification paper failed:', err.message))
}
```

触发条件：`profileSummary.isEffective`（报告有效）且 `pendingBottlenecks.length > 0`（有待验证卡点）。

### 3.2 覆盖策略

触发自动生成前，先把该学科已有的"未验证验证卷"标记为 `superseded`：

```js
async function triggerAutoVerificationPaper(reportId, studentId, subject, profile) {
  // 1. 覆盖旧卷：标记同学科未验证的旧验证卷为 superseded
  const oldPapers = await db.collection('papers')
    .where({ studentId, subject, type: 'verification', generationStatus: db.command.neq('superseded') })
    .get()
  for (const old of oldPapers.data) {
    // 如果旧卷还没验证过（没有对应的验证报告），才覆盖
    const hasVerification = await hasVerificationReport(studentId, subject, old._id)
    if (!hasVerification) {
      await db.collection('papers').doc(old._id).update({
        data: { generationStatus: 'superseded', supersededAt: new Date(), supersededBy: reportId }
      })
    }
  }

  // 2. 创建新验证卷记录（状态 generating）
  const targets = extractTargets(profile)  // 从 pendingBottlenecks 提取卡点列表
  const paperId = await createPaperRecord({
    studentId, subject, type: 'verification', targets,
    generationStatus: 'generating', autoGenerated: true,
    triggeredByReport: reportId
  })

  // 3. 调生成逻辑（带重试）
  await generateWithRetry(paperId, studentId, subject, targets, 0)
}
```

### 3.3 失败重试机制

```js
async function generateWithRetry(paperId, studentId, subject, targets, attempt) {
  const MAX_ATTEMPTS = 3
  const RETRY_DELAYS = [0, 10000, 20000]  // 首次立即、10s后、20s后

  try {
    // 调用 generatePaper 的核心逻辑（提取为可复用函数）
    const result = await generateVerificationPaperCore(studentId, subject, targets)
    await db.collection('papers').doc(paperId).update({
      data: { generationStatus: 'ready', generationAttempts: attempt + 1, ...result }
    })
  } catch (err) {
    if (attempt + 1 < MAX_ATTEMPTS) {
      // 记录失败，等待后重试
      await db.collection('papers').doc(paperId).update({
        data: { generationAttempts: attempt + 1, generationError: err.message }
      })
      await new Promise(r => setTimeout(r, RETRY_DELAYS[attempt + 1]))
      return generateWithRetry(paperId, studentId, subject, targets, attempt + 1)
    } else {
      // 最终失败
      await db.collection('papers').doc(paperId).update({
        data: { generationStatus: 'failed', generationAttempts: attempt + 1, generationError: err.message }
      })
    }
  }
}
```

### 3.4 云函数超时处理

`analyzePhotos` 本身可能已经运行了较长时间，剩余时间不够调 `generatePaper`（LLM 需要 30-60 秒）。

两种策略（实现时选其一）：
- **策略 A（优先）**：如果 `analyzePhotos` 剩余时间充足（>60s），直接在函数内调生成。通过 `cloud.callFunction({ name: 'generatePaper', data: {...} })` 调子函数，避免代码重复。
- **策略 B（兜底）**：如果剩余时间不足，只创建 `generationStatus: 'generating'` 的 paper 记录，依赖下次 `getAnalysisProgress` 轮询时触发补生成。

推荐策略 A——`analyzePhotos` 配置了 60 秒超时，分析本身通常在 20-30 秒完成，剩余时间足够调 `generatePaper`。如果不够，降级到策略 B。

## 四、数据模型变更

### 4.1 `papers` 表新增字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `autoGenerated` | boolean | 是否自动生成（true）vs 用户手动生成（false） |
| `generationStatus` | string | 生成状态：`generating` / `ready` / `failed` / `superseded` |
| `generationAttempts` | number | 重试次数（0=首次尝试，1/2=重试） |
| `generationError` | string | 失败原因（成功时为空） |
| `triggeredByReport` | string | 触发自动生成的诊断报告 ID |
| `supersededAt` | date | 被覆盖的时间 |
| `supersededBy` | string | 覆盖它的新诊断报告 ID |

### 4.2 状态机

```
generating ──成功──→ ready
    │                   │
    │失败重试            │用户上传作答
    ↓                   ↓
  failed            (验证报告生成后，该卷"完成使命")
    │
    │用户手动重试
    ↓
  generating（重新进入循环）
```

`superseded` 是终态——被新诊断报告覆盖的旧卷，不再激活。

### 4.3 "当前激活的验证卷"定义

一个学生 + 一个学科，同时只有一份 `generationStatus: 'ready'` 的验证卷是"激活的"。查询逻辑：

```js
// studentData 云函数新增 action
async function getActiveVerificationPaper(studentId, subject) {
  const res = await db.collection('papers')
    .where({ studentId, subject, type: 'verification' })
    .orderBy('createdAt', 'desc')
    .limit(5)
    .get()

  // 优先找 ready 状态的
  const ready = res.data.find(p => p.generationStatus === 'ready')
  if (ready) return { paper: ready, status: 'ready' }

  // 其次找 generating
  const generating = res.data.find(p => p.generationStatus === 'generating')
  if (generating) return { paper: generating, status: 'generating' }

  // 其次找 failed
  const failed = res.data.find(p => p.generationStatus === 'failed')
  if (failed) return { paper: failed, status: 'failed' }

  // 没有任何自动生成的卷
  return { paper: null, status: 'none' }
}
```

## 五、前端入口改造方案

### 5.1 改造原则

**所有"生成验证卷"入口，改为"状态分流"**：

```
入口点击 → 调 getActiveVerificationPaper(studentId, subject)
  → status === 'ready'    → 跳 paper-preview（直接预览/打印）
  → status === 'generating' → 提示"验证卷生成中…"（禁用或跳预览页显示进度）
  → status === 'failed'     → 跳 generate-verification（手动重试）
  → status === 'none'       → 跳 generate-verification（兜底手动生成）
```

### 5.2 逐页面改造清单

#### 报告页 `report`（2 个入口）

| 原按钮 | 改后（status=ready） | 改后（status=generating） | 改后（status=failed） | 改后（status=none） |
|--------|---------------------|--------------------------|----------------------|---------------------|
| 底部"＋生成纸面验证卷" | "查看验证卷"→跳预览 | "验证卷生成中…"→禁用 | "点击重试"→跳生成页 | "生成验证卷"→跳生成页 |
| 结论卡"生成纸面验证卷" | 同上 | 同上 | 同上 | 同上 |

改动文件：
- `report-presenter.js`：`showNextStep` 逻辑加 paper 状态判断，`explanationActionType` 根据 status 变化
- `report.js`：`onGenerateVerification` 改为状态分流跳转

#### 学科工作台 `subject-home`（1 个入口）

主任务卡按钮文案随 paper 状态变化：
- ready → **"验证卷已就绪，去打印"**
- generating → **"验证卷生成中…"**
- failed → **"验证卷生成失败，重试"**
- none → 保持原来的 **"生成纸面验证卷"**

改动文件：
- `subject-home-presenter.js`：`buildPrimaryTask` 加 paper 状态参数
- `subject-home.js`：`navigateToVerification` 改为状态分流

#### 卡点中心 `bottleneck-center`（1 个入口）

每个卡点卡片的"验证"按钮：
- 该学科已有 ready 验证卷 → 按钮变成 **"查看验证卷"**，跳 paper-preview
- 否则保持"验证"，跳 generate-verification

改动文件：`bottleneck-center.js`：`onGenerateForBottleneck` 加状态判断

#### 卡点详情 `bottleneck-detail`（1 个入口）

底部"生成纸面验证卷"按钮：逻辑同学科工作台。
关联验证卷展示（`onViewPaper`）**不用改**——它本来就是展示已生成的卷。

改动文件：`bottleneck-detail.js`：`onGenerateVerification` 加状态判断

#### 首页 / 档案页（2 个入口 via shared-navigation）

`shared-navigation.js` 的 `onBottleneckAction` 和 `onPrimaryAction`：
- 原来 `primaryText === '生成纸面验证卷'` 时跳 generate-verification
- 改为：先查 paper 状态，ready 跳 paper-preview，否则跳 generate-verification

改动文件：
- `shared-navigation.js`：两个函数加状态分流
- `index-presenter.js`：`primaryText` 判断加 paper 状态

### 5.3 不改的页面

| 页面 | 原因 |
|------|------|
| `paper-preview` | 已支持 paperId 直进，自动生成的卷子天然落地这里 |
| `generate-verification` | 保留作为兜底/重试入口，加"系统已自动生成"提示 |
| `upload`（验证作答上传） | 不涉及验证卷生成逻辑 |

### 5.4 新增前端方法

`miniprogram/utils/cloud.js` 新增：

```js
/**
 * 查询当前激活的验证卷状态
 * @returns { paper: object|null, status: 'ready'|'generating'|'failed'|'none' }
 */
async function getActiveVerificationPaper(studentId, subject) {
  return callFunction('studentData', {
    action: 'getActiveVerificationPaper', studentId, subject
  })
}
```

各页面 `onShow` 时调一次，根据返回决定按钮文案和跳转目标。

## 六、generate-verification 页面的角色变化

这个页面**不删除**，但从"主流程页面"降级为"兜底/重试页面"：

| 场景 | 用户如何进入 | 页面行为 |
|------|-------------|----------|
| 自动生成失败 | 入口 status=failed → 跳生成页 | 显示"自动生成失败，请手动选择卡点重新生成" |
| 用户想手动选卡点 | 入口 status=none → 跳生成页 | 正常流程（选卡点→生成） |
| 用户想覆盖已有卷 | 主动从某处进入 | 显示"系统已自动生成验证卷（X月X日），你可以重新选择卡点生成新的" |

## 七、实现拆分

### 第一阶段：后端（自动生成引擎）

| 步骤 | 文件 | 内容 |
|------|------|------|
| 1 | `cloudfunctions/generatePaper/index.js` | 提取 `generateVerificationPaperCore` 可复用函数 |
| 2 | `cloudfunctions/analyzePhotos/index.js` | `writeCompletedAnalysis` 结尾挂载 `triggerAutoVerificationPaper` |
| 3 | `cloudfunctions/analyzePhotos/` 或新模块 | 实现 `triggerAutoVerificationPaper` + `generateWithRetry` + 覆盖旧卷逻辑 |
| 4 | `cloudfunctions/studentData/index.js` | 新增 `getActiveVerificationPaper` action |
| 5 | 测试 | 新增 `auto-verification.test.js`：验证触发、覆盖、重试、状态查询 |

### 第二阶段：前端（入口改造）

| 步骤 | 文件 | 内容 |
|------|------|------|
| 1 | `miniprogram/utils/cloud.js` | 新增 `getActiveVerificationPaper` |
| 2 | `miniprogram/utils/shared-navigation.js` | `onBottleneckAction`/`onPrimaryAction` 加状态分流 |
| 3 | `miniprogram/pages/report/` | `report.js` + `report-presenter.js` 改按钮和跳转 |
| 4 | `miniprogram/pages/subject-home/` | `subject-home.js` + presenter 改主任务卡 |
| 5 | `miniprogram/pages/bottleneck-center/` | `onGenerateForBottleneck` 加状态判断 |
| 6 | `miniprogram/pages/bottleneck-detail/` | `onGenerateVerification` 加状态判断 |
| 7 | `miniprogram/pages/index/` | `index-presenter.js` 改 `primaryText` 判断 |
| 8 | `miniprogram/pages/generate-verification/` | 加"已自动生成"提示 |
| 9 | 测试 | 更新 `page-flows.test.js` 等覆盖新跳转逻辑 |

## 八、风险与注意事项

| 风险 | 影响 | 缓解 |
|------|------|------|
| LLM 生成时间不可控（可能 >60s） | 云函数超时 | 重试机制 + 策略 B 兜底（标记 generating，下次轮询补生成） |
| 用户连续上传多份诊断报告 | 触发多次自动生成 | 覆盖策略：新卷标记旧卷 superseded，始终只激活一份 |
| 自动生成消耗 LLM 配额 | 成本增加 | 只在有 pendingBottlenecks 时触发；失败重试有上限（3 次） |
| 前端入口改造范围大 | 回归风险 | 分阶段实现，先改报告页和学科工作台（最高频入口），再改其他 |

## 九、完整文件清单

### 新增文件
| 文件 | 说明 |
|------|------|
| `tests/auto-verification.test.js` | 自动生成引擎测试 |

### 后端改动文件
| 文件 | 改动 |
|------|------|
| `cloudfunctions/analyzePhotos/index.js` | `writeCompletedAnalysis` 结尾挂载自动生成 |
| `cloudfunctions/generatePaper/index.js` | 提取 `generateVerificationPaperCore` 可复用函数 |
| `cloudfunctions/studentData/index.js` | 新增 `getActiveVerificationPaper` action |

### 前端改动文件
| 文件 | 改动 |
|------|------|
| `miniprogram/utils/cloud.js` | 新增 `getActiveVerificationPaper` |
| `miniprogram/utils/shared-navigation.js` | 两个函数加状态分流 |
| `miniprogram/pages/report/report.js` | 状态分流跳转 |
| `miniprogram/pages/report/report-presenter.js` | 按钮文案随状态变化 |
| `miniprogram/pages/subject-home/subject-home.js` | 状态分流跳转 |
| `miniprogram/pages/subject-home/subject-home-presenter.js` | 主任务卡文案 |
| `miniprogram/pages/bottleneck-center/bottleneck-center.js` | 按钮状态判断 |
| `miniprogram/pages/bottleneck-detail/bottleneck-detail.js` | 按钮状态判断 |
| `miniprogram/pages/index/index-presenter.js` | primaryText 判断 |
| `miniprogram/pages/generate-verification/generate-verification.js` | 加"已自动生成"提示 |

### 不改的文件
| 文件 | 原因 |
|------|------|
| `paper-preview.js` + presenter | 已支持 paperId 直进，无需改 |
| `upload.js` | 不涉及验证卷生成 |
| `traceable-actions.js` | 只做 URL 拼接，无业务逻辑 |

---

## 十、v2 升级：全量细卡点 + 统一入口 + 异步自动生成（2026-06-20）

> 版本：v2.0
> 状态：已实现，534/534 测试通过

### 10.1 v1 遗留问题

v1 的自动生成引擎已上线，但存在三个问题：

1. **手动入口仍用旧逻辑**：诊断报告/卡点详情/学科首页等 6 个入口的"生成验证卷"按钮，仍跳转 `generate-verification` 页，该页有三道硬上限：`MAX_SELECTED_BOTTLENECKS=10`、`QUESTIONS_PER_BOTTLENECK=2`、`MAX_TOTAL_QUESTIONS=20`。导致自动生成失败回退到手动页时，验证卷只有 10 卡点 20 题。
2. **单次调用超时**：自动生成引擎走 `generatePaper` 的 `_autoPaperId` 单次模式，38 个细 BN + 置信度分层（高3/中2/低1）会产生 60-90 题，单次 60 秒必然超时。
3. **expectedCount 与置信度矛盾**：`generatePaper/index.js` 的 `expectedCount = targets.length × 2`（固定每卡点2题），与 prompt 里按置信度分层（3/2/1题）矛盾，导致 `normalizeQuestionsData` 数量校验截断或报错。

### 10.2 v2 核心改造

#### 改造一：自动生成短任务模式（`auto-verification.js` + `regenerateVerificationPaper`）

- `triggerAutoVerificationPaper` 只创建 `generating` 记录并回写报告关联；不再在 `analyzePhotos` 里同步跑完整份验证卷
- `regenerateVerificationPaper?action=continue` 每次只推进 1 个未生成 BN，成功后由后端调度下一次 `continue`
- 每个 BN 调用 `generatePaper` 的 `_appendToPaperId` 模式只追加题目，不生成 PDF
- 全部 BN 成功后，最后一次 `continue` 调用 `generatePaper` 的 `_regeneratePdf` 模式生成最终 PDF
- 单个 BN 失败会独立重试；最终失败时写入 `failed`，记录错误信息，不再继续生成最终 PDF 或标记 ready
- `paper.generationStatus` 流转：`generating → appending → ready`；失败路径为 `generating/appending → failed`，含 `generationProgress`（completedBatches/totalBatches/succeededBatches/failedBatches）

#### 改造二：expectedCount 按置信度累加（`generatePaper/index.js`）

```
// 改前：expectedCount = targets.length × 2（固定）
// 改后：expectedCount = Σ questionsForWeight(weight)
//   weight≥75 → 3 题，45-74 → 2 题，<45 → 1 题
```

与 prompt 的置信度标签 `[高置信·出3题]` 完全一致，`normalizeQuestionsData` 的数量校验阈值同步对齐。

#### 改造三：新增 `regenerateVerificationPaper` 云函数

独立云函数，用于创建和续跑验证卷：
- 查 profile → 提取全量细 BN（与 `auto-verification.js` 的 `extractFineBottlenecks` 逻辑一致）
- 创建 `generating` 状态的 paper 记录
- `action=start` 返回 paperId 和目标列表，兼容维护入口
- `action=continue` 读取 paper 当前题目，跳过已生成 BN，只推进下一个未生成 BN
- 最后一个 BN 完成后自动 `_regeneratePdf`，并将 paper/report 标记为 ready
- 因云函数独立打包无法跨函数 require，此处内联核心逻辑

#### 改造四：统一入口纯函数（`shared-navigation.js`）

抽取 `navigateToVerificationPaper(cloudModule, { studentId, subject, reportId })` 纯函数：

| 状态 | 行为 |
|------|------|
| `ready` | 直接跳 `paper-preview` |
| `generating` | Toast 提示 + 启动轮询（5秒/次，最多24次=2分钟），ready 后自动跳预览 |
| `failed` | 提示后台生成失败 |
| `none` | 提示暂无验证卷 |

所有页面的"查看验证卷"按钮统一调用此函数。

#### 改造五：入口统一 + 按钮文案

| 页面 | 改动 |
|------|------|
| `report.js` | 按钮"生成纸面验证卷"→"查看验证卷"；onLoad 自动轮询（诊断报告且有卡点时静默检查） |
| `bottleneck-detail.js` | 按钮文案改"查看验证卷"；调统一函数 |
| `subject-home.js` | 调统一函数 |
| `bottleneck-center.js` | 调统一函数 |
| `report-presenter.js` | `explanationActionText` 改"查看验证卷" |

#### 改造六：`generate-verification` 页降级为兜底

- 删除 `MAX_SELECTED_BOTTLENECKS=10`、`QUESTIONS_PER_BOTTLENECK=2`、`MAX_TOTAL_QUESTIONS=20` 硬上限
- 卡点默认全选，展示置信度标签（`高置信·3题` / `中置信·2题` / `低置信·1题`）
- `questionCountForSelection` 改为按 weight 累加（`Σ questionsForWeight`）
- 仅在旧环境（cloud 不可用）或极端兜底时进入

### 10.3 数据流

```
诊断报告完成
  ↓
analyzePhotos/index.js (mode !== 'verification' && has pendingBottlenecks)
  ↓
triggerAutoVerificationPaper
  ↓
extractFineBottlenecks → 全量细 BN（按 weight 降序）
  ↓
createGeneratingPaper (generationStatus: 'generating')
  ↓ 回写 report.verificationPaperId
scheduleVerificationContinuation
  ↓
regenerateVerificationPaper?action=continue:
  ├─ 读取 paper.questions，跳过已生成 BN
  ├─ 推进下一个 BN: generatePaper(_appendToPaperId, [target])
  ├─ 更新 generationProgress
  ├─ 还有 BN → 调度下一次 continue
  ├─ 任一 BN 最终失败 → generationStatus: failed
  └─ 全部成功后 generatePaper(_regeneratePdf) → 最终 PDF
  ↓
generationStatus: 'ready' + 回写 report.verificationPaperStatus: 'ready'

用户点击"查看验证卷"（任意入口）
  ↓
navigateToVerificationPaper
  ├─ getActiveVerificationPaper(reportId)
  ├─ ready → 跳 paper-preview
  ├─ generating → 轮询（5s/次），ready 后自动跳
  ├─ failed → 提示后台生成失败
  └─ none → 提示暂无验证卷
```

### 10.4 关键配置

| 配置项 | 值 | 位置 |
|--------|-----|------|
| `BATCH_SIZE` | 1 个 BN/批 | `auto-verification.js`、`regenerateVerificationPaper/index.js` |
| `MAX_RETRY_ATTEMPTS` | 3 次/批 | 同上 |
| `VERIFICATION_POLL_INTERVAL` | 5000ms | `shared-navigation.js` |
| `VERIFICATION_POLL_MAX_ATTEMPTS` | 24 次（2分钟） | 同上 |
| 置信度阈值 | 75/45 | `generatePaper/index.js`、`bottleneck-view.js`、`generate-verification.js` |

### 10.5 v2/v4 改动文件清单

**云函数（需部署）：**
- `cloudfunctions/analyzePhotos/auto-verification.js` — 创建 generating 记录、按 weight 排序、调度短任务续跑
- `cloudfunctions/generatePaper/index.js` — expectedCount 按置信度累加
- `cloudfunctions/regenerateVerificationPaper/` — `continue` 短任务续跑，每次推进 1 个 BN，最终重排 PDF

**前端（需 recompile）：**
- `miniprogram/utils/shared-navigation.js` — 统一纯函数 + 轮询
- `miniprogram/utils/cloud.js` — `regenerateVerificationPaper` 封装
- `miniprogram/pages/report/` — 按钮文案 + onLoad 轮询
- `miniprogram/pages/bottleneck-detail/` — 统一入口
- `miniprogram/pages/subject-home/` — 统一入口
- `miniprogram/pages/bottleneck-center/` — 统一入口
- `miniprogram/pages/generate-verification/` — 降级兜底 + 置信度题量
