# Node modules - cloud functions
cloudfunctions/**/node_modules/
# Root node_modules (dev tooling only, e.g. miniprogram-automator)
node_modules/

# WeChat DevTools private config (contains personal settings, not for version control)
project.private.config.json

# macOS
.DS_Store
**/.DS_Store

# Logs
*.log
npm-debug.log*
.superpowers/
tmp/

# Codemap tool transient outputs (the .md artifacts in docs/CODEMAPS/ are kept)
.reports/

# Environment / secrets
.env
.env.local
*.pem

# Backups
.backup-*/
data/**/.backup-*/
